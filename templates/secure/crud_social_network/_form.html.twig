{{ form_start(form) }}
    {{ form_widget(form) }}
    <button style="width: 90px" class="btn btn-primary">Guardar</button>
    <a style="width: 90px" class="btn btn-secondary" href="{{ path('secure_crud_social_network_index') }}">Regresar</a>
{{ form_end(form) }}
