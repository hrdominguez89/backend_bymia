{% extends 'home/index.html.twig' %}
{% block title %}
	{{title}}
{% endblock %}

{% block content %}
	{{ parent() }}

	<div class="d-flex justify-content-between">
		<div class="col-6">
			<h1 class="mr-auto mb-0 title-table">{{title}}</h1>
		</div>
	</div>
	<div class="row mt-5">
		<div class="col-12">
			<div class="table-responsive">
				<table id="table_full_buttons" class="table table-striped table-sm nowrap" data-order="[[ 2, &quot;asc&quot; ]]">
					<thead>
						<tr>
							<th data-type="num" class="text-center">
								Id. &nbsp;
							</th>

							<th data-type="num" class="text-center">
								Id 3PL &nbsp;
							</th>

							<th>
								Nombre &nbsp;
							</th>

							<th>
								Slug &nbsp;
							</th>

							<th data-type="num" class="text-center">
								Subcategorías relacionadas &nbsp;
							</th>

							<th data-orderable="false">
								Imagen &nbsp;
							</th>
							{# <th class="text-center">
																						Visible &nbsp;
																					</th> #}

							<th class="text-center" data-orderable="false">
								Acciones &nbsp;
							</th>

						</tr>
					</thead>
					<tbody>
						{% for category in categories %}

							<tr>

								<td class="text-center">
									{{category.id}}
								</td>


								<td class="text-center">
									{{category.apiId}}
								</td>

								<td>
									{{category.name}}
								</td>

								<td>
									{{category.slug}}
								</td>

								<td class="text-center">
									{{category.cantidad}}
								</td>

								<td>
									{% if category.image %}
										<img class="imagen-grid" alt="Imagen" src="{{ asset(category.image) }}"/>
									{% else %}
										<img class="imagen-grid" alt="Imagen" src="{{ asset('uploads/images/default.jpg') }}"/>
									{% endif %}
								</td>
								{#
								                                <td>
								                                    <a style="font-size:16px" data-status="{{country.visible ? 'on':'off'}}" data-slug="/secure/world/updateVisible/Countries" data-id="{{country.country_id}}" class="text-{{country.visible ? 'success':'secondary'}} m-2 toggle-on-off" href="javascript:void(0);">
								                                    {{brand.visible}}
								                                        <i class="fas fa-toggle-{{country.visible ? 'on':'off'}}"></i>
								                                    </a>
								                                </td>#}


								<td class="text-center">
									<a style="font-size:16px" class="text-warning m-2" href="{{ path('secure_crud_category_edit', {'id': category.id}) }}" title="Editar categoría">
										<i class="fas fa-edit"></i>
									</a>
								</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</div>
		</div>
	</div>
	<div class="row mt-5 mb-5">
		<div class="col-12 text-end">
			<a class="btn btn-secondary" href="{{ path('app_homepage') }}">Regresar</a>
		</div>
	</div>
{% endblock %}
{% block javascripts %}
	{{ parent() }}
{% endblock %}
{# 


{% extends 'home/index.html.twig' %}

{% block title %}Categoría{% endblock %}

{% block content %}
<div class="d-flex justify-content-between">
    <h1 class="mr-auto mb-0 title-table">Categoría</h1>
    <a class="btn btn-success" href="{{ path('secure_crud_category_new') }}">Crear nueva</a>
</div>
    <table class="table imagen">
        <thead>
            <tr>
                <th>Imagen</th>
                <th>Nombre</th>
                <th>Cantidad de productos</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr>
                {% if category.image %}
                    <td><img class="imagen-grid" alt="Imagen" src="{{ asset(category.image) }}"/></td>
                {% else %}
                    <td><img class="imagen-grid" alt="Imagen" src="{{ asset('uploads/images/default.jpg') }}"/></td>
                {% endif %}
                <td>{{ category.name }}</td>
                <td>{{ category.items }}</td>
                <td style="width: 300px">
                    <a style="width: 70px" class="btn btn-info" href="{{ path('secure_crud_category_show', {'id': category.id}) }}">Mostrar</a>
                    <a style="width: 70px" class="btn btn-warning" href="{{ path('secure_crud_category_edit', {'id': category.id}) }}">Editar</a>
                    <a style="width: 108px" class="btn btn-outline-secondary" href="{{ path('secure_crud_subcategory_index', {'id_category': category.id}) }}">Subcategoria</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="10">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div class="navigation">
        {{ knp_pagination_render(categories) }}
    </div>
{% endblock %} #}
